program testFunDir;

#class Persona {
#    attributes
#        int : iAge;
#        float : fHeight;
#        char : cFirst, cMid, cLast;
#};

variables {
    int : iCount;
    float : fResult;
    char : cInitial;
    Persona : Juan;
}

int function random(cKey : char) {
    variables {
        int : iRes;
        float : fNada;
    }

    if (cKey == 'A') then {
        iRes = 1;
    } else {
        if (cKey == 'B') then{
            iRes = 2;
        } else {
            iRes = 0;
        }
    }

    return(iRes);
}


float function promedio(fSuma : float, iTotal : int) {
    variables {
         float : fResult;
         char : cEmpty;
    }

    fResult = fSuma / iTotal;

    return (fResult);
}


char function clasifica() {
    variables {
        char : cRating;
    }

    cRating = 'R';

    return(cRating);
}

void function hello() {
    write("Hello world!");
}

main() {

}