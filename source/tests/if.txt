program ifTest;

variables {
    int: a, b, c, T, F, out, out2;
}

main() {
    # if simple
    if(a != b) then {

        if (b != a) then {
            b = a + b;

        } else {
            b = a + a;
            # write("lmao");
        }
        out = out + out;
    }
    out2 = out2 + out2;

    # if-else
    if(33+77 == 100) then {
        T = a + b;

    } else { # comment en medio
        F = a + b;
    }
}