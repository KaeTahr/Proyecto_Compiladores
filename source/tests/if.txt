program ifTest;

variables {
    int: a, b;
}

main() {
    # if simple
    if(a != b) then {
        return(a);
    }

    # if-else
    if(33+77 == 100) then {
        return(1);
    } else { # comment en medio
        return(0);
    }
}