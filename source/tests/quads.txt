program testQuads;

variables {
    int : y, z;
    float : x, a, b, c, d, e;
}

main() {
    #x = a + y * b - z / c;
    #x = a + y > b - z || z >= c;
    #x = a + 2.5;
    x = a * b + (d - e) / c;
}