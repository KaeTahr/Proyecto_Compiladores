program voidCall;

int function promedio(tExams : int) {
    variables {
       ans : float;
       ans2 : int;
    }
    ans = tExams / tExams;

    return (promedio / tExams);
}

void function hello() {
    write("Hello world!");
}

main() {
    # esto es un comment
    x = juan.getEdad();
    y = promedio(examenes);

    hello(x[5,5], y, hello());


}